<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{htmlTitle}}</title>
    <link rel="stylesheet" href="{{projectAssets}}/vendor/bootstrap/css/bootstrap.css">
    <link rel="stylesheet" href="{{yuiGridsUrl}}">
    <link rel="stylesheet" href="{{projectAssets}}/vendor/prettify/prettify-min.css">
<!--     <link rel="stylesheet" href="{{projectAssets}}/css/main.css" id="site_styles">
 -->    <link rel="stylesheet" href="{{projectAssets}}/css/custom.css">
    <link rel="shortcut icon" type="image/png" href="{{projectAssets}}/favicon.png">
</head>
<body>
  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <div class="navbar-header">
        <a href="#" class="navbar-brand">
          {{#if projectLogo}}
            <img alt="{{projectName}}" src="{{projectLogo}}" title="{{projectName}}">
          {{else}}
            {{projectName}}
          {{/if}}
        </a>
      </div>
      <!--<p class="navbar-text">
        API Docs for Version: <b>{{projectVersion}}</b>
      </p>
       <form class="navbar-form pull-right">
        <input type="text" class="form-control" placeholder="Search for classes/modules..." data-obj='[{{#search classes allModules}}{{/search}}]'>
      </form> -->
    </div>
  </nav>
<div class="container">
  <div class="row">
      <div class="col-xs-3">
    {{>sidebar}}
      </div>
      <div class="col-xs-9">
          {{>options}}
          <div class="apidocs">
              <div id="docs-main">
                  <div class="content">
                      {{>layout_content}}
                  </div>
              </div>
          </div>
      </div>
  </div>
</div>
<script src="{{projectAssets}}/vendor/jquery/jquery-1.11.2.js"></script>
<script src="{{projectAssets}}/vendor/bootstrap/js/bootstrap.js"></script>
<script src="{{projectAssets}}/vendor/prettify/prettify-min.js"></script>
<script src="{{projectAssets}}/js/yuidoc-bootstrap.js"></script>
<script>prettyPrint();</script>
</body>
</html>